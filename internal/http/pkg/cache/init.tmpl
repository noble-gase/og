package cache

import "github.com/redis/go-redis/v9"

var uc redis.UniversalClient

func Init() {
	uc = New("redis")
}

func R() redis.UniversalClient {
	if uc == nil {
		panic(ErrClientNil)
	}
	return uc
}
